manifestVersion: 1
name: "@mgreten/storage-health"
version: "2026.07.16.1"
description: >
  Monitor disk usage and backup snapshot inventory on any SSH-accessible host
  or the local machine. Produces typed, versioned artifacts for each mount
  point and backup directory, with configurable warning and critical thresholds.

repository: https://github.com/meagerfindings/swamp-storage-health

models:
  - storage_health.ts

additionalFiles:
  - README.md
  - LICENSE.txt

platforms: []
